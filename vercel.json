{
  "version": 2,
  "buildCommand": "npm run vercel-build",
  "installCommand": "npm ci --include=dev || npm install --include=dev --legacy-peer-deps",
  "framework": "nextjs",
  "github": {
    "silent": true
  }
}

